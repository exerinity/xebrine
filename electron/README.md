# Xebrine Electron wrapper
This is a basic Electron wrapper that adds Linux MPRIS support and... basically not much else

I don't recommend you use this, but if you *really* want to build it, just do `npm run dist`

Oh yeah, it's really prone to memory leaks. And basically anything else. Don't use this shit. Use it in your browser this is just an experiment